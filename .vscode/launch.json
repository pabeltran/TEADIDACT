{
    // Usa IntelliSense para conocer los posibles atributos.
    // Pasa el cursor sobre los atributos existentes para ver descripciones.
    // Para más información, visita: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",  // Tipo de depurador: Chrome
            "request": "launch",  // Solicitud de acción: Lanzar Chrome
            "name": "Launch Chrome against localhost",  // Nombre de la configuración: Lanzar Chrome contra localhost
            "url": "http://localhost:8080",  // URL a la que Chrome se conectará: http://localhost:8080
            "webRoot": "${workspaceFolder}"  // Raíz web del proyecto: La carpeta del espacio de trabajo
        }
    ]
}